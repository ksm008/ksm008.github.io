---
layout: post
title: 30804번 - 과일 탕후루
date: 2026-02-09 17:45:27 +0900
categories:
  - 백준
  - 기타 알고리즘
published: false
math: true
tags:
  - 백준
  - 두_포인터
---
브루트포스로는 절대 풀 수 없는 문제이다. N은 최대 20만, 모든 구간을 전부 계산하려면 이중 반복문을 써야 하므로 연산 횟수는 400억번. 대략 1초에 처리 가능한 시간이 1억번 정도이기 때문에 무조건 시간 초과가 나온다. 따라서 무조건 두 포인터 알고리즘을 써야 한다.

두 포인터 알고리즘은 왼쪽과 오른쪽 포인터를 사용한다. 반복문 변수가 오른쪽 포인터, 왼쪽 포인터는 외부에서 선언해 따로 사용한다. 반복문을 반복하며 특정 조건을 만족하게 된다면 왼쪽 포인터를 축소시키며 범위를 잘라낸다. 

```java
import java.io.*;  
import java.util.StringTokenizer;  
  
public class Main {  
    static BufferedReader br = new BufferedReader(new InputStreamReader(System.in));  
    static BufferedWriter bw = new BufferedWriter(new OutputStreamWriter(System.out));  
    static StringBuilder sb = new StringBuilder();  
  
    public static void main(String[] args) throws IOException {  
        int N = Integer.parseInt(br.readLine());  
        StringTokenizer st = new StringTokenizer(br.readLine());  
        int[] fruitTanghuru = new int[N];  
        int[] cnt = new int[10];  
        int L = 0;  
        int fruitType = 0;  
        int result = 0;  
  
        for (int i = 0; i < N; i++) {  
            int cur = Integer.parseInt(st.nextToken());  
            fruitTanghuru[i] = cur;  
            cnt[fruitTanghuru[i]]++;  
            if (cnt[fruitTanghuru[i]] == 1) {  
                fruitType++;  
            }  
  
            while (fruitType > 2) {  
                cnt[fruitTanghuru[L]]--;  
                if (cnt[fruitTanghuru[L]] == 0) {  
                    fruitType--;  
                }  
                L++;  
            }  
  
            result = Math.max(i - L + 1, result);  
        }  
  
        sb.append(result);  
        bw.write(sb.toString());  
        bw.flush();  
        bw.close();  
        br.close();  
    }  
}
```