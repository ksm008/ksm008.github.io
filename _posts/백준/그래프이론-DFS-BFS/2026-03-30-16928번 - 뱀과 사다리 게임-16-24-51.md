---
layout: post
title: 16928번 - 뱀과 사다리 게임
date: 2026-03-30 16:24:51 +0900
categories:
  - 백준
  - 그래프 이론
  - DFS/BFS
published: false
math: true
tags:
  - 백준
  - 그래프이론
  - BFS
---
클래스 3++의 문제이다. 나는 BFS로 풀었다. 처음에는 바보같이 정수 배열을 담은 리스트를 사용해서 시간 초과가 났는데... 사다리와 뱀 정보를 같이 담은 보드 정수 배열을 사용하여 시간을 줄였다. 당연히 이렇게 하는게 맞기야 하지만...
이제 BFS는 손쉽게 풀 수 있는 정도가 되긴 했다. 다른 알고리즘보다 쉬워서 그런가, 머릿속에 구상도가 잘 그려지는 느낌이다.

```java
import java.io.*;  
import java.util.*;  
  
public class Main {  
    static BufferedReader br = new BufferedReader(new InputStreamReader(System.in));  
    static BufferedWriter bw = new BufferedWriter(new OutputStreamWriter(System.out));  
    static StringBuilder sb = new StringBuilder();  
  
    static int N, M;  
    static int[] board;  
  
    static HashMap<Integer, Integer> map = new HashMap<>();  
  
    public static void main(String[] args) throws IOException {  
        StringTokenizer st = new StringTokenizer(br.readLine());  
  
        // 사다리랑 뱀을 한번에 저장하기. ladders랑 snakes로 안나눠도 된다...  
        board = new int[101];  
  
        for (int i = 1; i <= 100; i++) {  
            board[i] = i;  
        }  
  
        N  = Integer.parseInt(st.nextToken());  
        M  = Integer.parseInt(st.nextToken());  
  
        for (int i = 0; i < N; i++) {  
            st = new StringTokenizer(br.readLine());  
            int X = Integer.parseInt(st.nextToken());  
            int Y = Integer.parseInt(st.nextToken());  
            board[X] = Y;  
        }  
  
        for (int i = 0; i < M; i++) {  
            st = new StringTokenizer(br.readLine());  
            int U = Integer.parseInt(st.nextToken());  
            int V = Integer.parseInt(st.nextToken());  
            board[U] = V;  
        }  
  
        int result = bfs();  
  
        sb.append(result);  
        bw.write(sb.toString());  
        bw.flush();  
        bw.close();  
        br.close();  
    }  
  
    static int bfs() {  
        boolean[] visited = new boolean[101];  
        Deque<int[]> queue = new ArrayDeque<>();  
        // {현재 위치, 굴린 횟수}  
        queue.offer(new int[]{1, 0});  
        visited[1] = true;  
  
        while (!queue.isEmpty()) {  
            int[] curr = queue.poll();  
            int curPos = curr[0];  
            int curDice = curr[1] + 1;  
  
            for (int i = 1; i <= 6; i++) {  
                int newPos = curPos + i;  
  
                if (newPos > 0 && newPos <= 100) {  
                    newPos = board[newPos];  
                    if (!visited[newPos]) {  
                        visited[newPos] = true;  
                        queue.offer(new int[]{newPos, curDice});  
                    }  
                }  
  
                if (newPos == 100) {  
                    return curDice;  
                }  
            }  
        }  
  
        return -1;  
    }  
}
```